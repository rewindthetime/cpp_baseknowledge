#include <iostream>
using namespace std;

int main() {
    bool amIBeautifull = true;

    if (amIBeautifull = true) {
        cout << "<3 thanks for viewing this\n";
    } else {
        // if the condition is false (its not im very beautifull) okay enough.. 
        // your code here is going to get runned
        cout << "wait what..\n";
    }

    if (amIBeautifull = true) {
        cout << "<3 thanks for viewing this\n";
    } else if (amIBeautifull = "the other condition") {
        // your block of code <3
    } else {
        // else 
    }

    int age = 20;
    string result = (age > 18) ? "old man" : "small boi."; // i know how to spell english..
    cout << result;

    return 0;
}
